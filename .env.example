# FundLens Backend — no environment variables are required.
# The app fetches data itself from mfdata.in and caches it on disk (data_cache.json).
#
# Optional:
# PORT=8000        # port for uvicorn (most hosts set this automatically)
